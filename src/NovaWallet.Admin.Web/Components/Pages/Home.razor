@page "/"
@inject NavigationManager Nav

<PageTitle>NovaWallet Admin</PageTitle>

<div class="container text-center mt-5">
    <h1>NovaWallet Admin</h1>
    <p class="lead">Administrative portal for transaction monitoring, settlement review, and reconciliation.</p>
    <a href="/login" class="btn btn-primary btn-lg mt-3">Admin Login</a>
</div>

@code { protected override void OnInitialized() => Nav.NavigateTo("/login", forceLoad: true); }
